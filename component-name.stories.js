import React from 'react';
import ComponentName from './component-name';
import data from './data';

export default { title: 'Paragraphs|Component Name' }

export const Default = () => (
  <ComponentName data={data} />
);
